import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Os arquivos de regras para agentes são mantidos manualmente neste repositório.
  agentRules: false,
};

export default nextConfig;
